[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18.16.0"  # Exact LTS version

[context.production.environment]
  GENERATE_SOURCEMAP = "false"
  CI = "false"
